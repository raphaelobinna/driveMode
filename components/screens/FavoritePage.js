import React from 'react';
import {
  View,
  Text,
  TouchableOpacity,
  Image,
  ImageBackground,
  SafeAreaView,
  Platform,
} from 'react-native';

export const FavoritePage = ({navigation, route}) => {
  return (
    <View>
      <Text>Favorite Screen</Text>
    </View>
  );
};
