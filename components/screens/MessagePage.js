import React from 'react';
import {
  View,
  Text,
  TouchableOpacity,
  Image,
  ImageBackground,
  SafeAreaView,
  Platform,
} from 'react-native';

export const MessagePage = ({navigation, route}) => {
  return (
    <View>
      <Text>Message Screen</Text>
    </View>
  );
};
