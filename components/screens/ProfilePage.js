import React from 'react';
import {
  View,
  Text,
  TouchableOpacity,
  Image,
  ImageBackground,
  SafeAreaView,
  Platform,
} from 'react-native';

export const ProfilePage = ({navigation, route}) => {
  return (
    <View>
      <Text>Profile Screen</Text>
    </View>
  );
};
